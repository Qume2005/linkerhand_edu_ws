# l10_hand_gateway 触觉防护配置
# 修改后重启节点生效

# 是否启用触觉防护
enabled: true

# 触发阈值 — 力矩阵单格最大值 (0-255)
# 当前设定: 0.1 = 矩阵任意一格非零即触发
# 调大此值可提高触发门槛，直接对应单格压力值
threshold: 0.1

# 回退步数
# 0   = 仅冻结 (不允许继续弯曲，允许伸直)
# >0  = 手指向伸直方向回退 N 个单位后冻结
retreat_units: 0.0

# 冻结持续时间 (秒)
# 超时后自动解除，并进入等长冷却期防止立即重新触发
freeze_duration: 5.0
